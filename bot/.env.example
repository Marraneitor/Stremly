# ============================================================
# Streamly Bot — Variables de Entorno (RAILWAY)
# ============================================================
# En Railway: Variables tab → agrega cada una.
# En local: copia como .env y rellena con tus valores.
# NUNCA subir este archivo con valores reales a git.
# ============================================================

# ── REQUERIDAS ───────────────────────────────────────────────

# API Key de Gemini (para respuestas del bot con IA)
# Obtener de: https://aistudio.google.com/apikey
GEMINI_API_KEY=tu_gemini_api_key

# Firebase Project ID
# Obtener de: Firebase Console → Project Settings → General
FIREBASE_PROJECT_ID=tu-proyecto-id

# UID del usuario dueño del negocio en Firebase Auth
# Obtener de: Firebase Console → Authentication → Users → copiar UID
BOT_OWNER_UID=tu_uid_de_firebase

# ── FIREBASE SERVICE ACCOUNT (para leer/escribir Firestore) ──
# Obtener de: Firebase Console → Project Settings → Service Accounts
#   → "Generate new private key" → copiar TODO el JSON
# En Railway: pegar el JSON completo como valor de esta variable
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}

# ── OPCIONALES ───────────────────────────────────────────────

# Puerto HTTP (Railway lo asigna automáticamente con $PORT)
# BOT_PORT=3001

# URL del frontend (si quieres sync bidireccional)
# FRONTEND_URL=https://streamly.vercel.app
